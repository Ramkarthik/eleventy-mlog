<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <meta name="Description" content="{{ title }}" />
    <meta name="og:title" content="{{ title }}" />
    <meta name="og:description" content="{{ content | strip_html slice: 0, 99 }}" />
    <meta name="twitter:title" content="{{ title }}" />
    <meta name="twitter:description" content="{{ content | strip_html slice: 0, 99 }}" />
    <meta name="twitter:site" content="@ramkarthik" />
    <title>{{ title }}</title>
    <link
      href="https://fonts.googleapis.com/css?family=IBM+Plex+Sans:300,400&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../styles/common.css" />
    <link rel="stylesheet" href="../../styles/blogpost.css" />
    <link rel="stylesheet" href="../../styles/prism-theme.css"/>
  </head>
  <body>
    <div class="container post-container">
      <div class="back-link">
        <a href="/">Home</a>
      </div>
        <h1>{{ title }}</h1>
      <div class='content'>{{ content }}</div>
      <div class="back-link">
        <a href="/">← Back</a>
      </div>
    </div>
  </body>
</html>